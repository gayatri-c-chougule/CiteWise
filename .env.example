# Example environment variables for CiteWise

# Where ChromaDB will store its persistent data (folder will be created if missing)
VECTOR_DB_PATH=./chroma_db

# Embedding model + device
EMBEDDING_MODEL=all-mpnet-base-v2
EMBEDDING_DEVICE=cpu   # cpu | cuda | mps (Apple Silicon)

# (Optional) If you later use OpenAI embeddings
OPENAI_API_KEY=your-openai-api-key-here
